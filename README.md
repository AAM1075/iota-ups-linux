# iota-ups-linux
Exposes the LattePanda IOTA's UPS board as a standard battery so it can be used without any special configuration

Normally, Linux will recognize the UPS board as a UPS, but in a lot of cases, it might be useful to have it appear to applications (such as a UPower) as a normal battery.
